version: '3.8'

services:
  frontend:
    build:
      target: prod
    ports:
      - "80:80"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M

  backend:
    deploy:
      resources:
        limits:
          memory: 512M